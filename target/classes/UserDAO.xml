<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liuxingyu.dao.UserDAO">
    <select id="queryById" resultType="com.liuxingyu.domain.User">
    SELECT * FROM TB_USER WHERE ID = #{ID}
    </select>
    <select id="queryAll" resultType="com.liuxingyu.domain.User">
    SELECT * FROM TB_USER
    </select>
</mapper>